export const CHANGE_ONE_LOG = (state, payload) => {
  state.oneLog = payload
}

export const CHANGE_TWO_LOG = (state, payload) => {
  state.twoLog = payload
}

export const CHANGE_THREE_LOG = (state, payload) => {
  state.threeLog = payload
}
